[workspace]
members = [
    "contracts/vfly-token",
    "contracts/veriflo-verifier",
    "contracts/kyc-verifier",
    "contracts/credential-registry",
]
resolver = "2"

[profile.release]
opt-level = "z"
lto = true
panic = "abort"
codegen-units = 1
overflow-checks = true
