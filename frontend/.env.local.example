# Public — safe to expose (contract addresses on testnet)
NEXT_PUBLIC_TOKEN_CONTRACT=C...
NEXT_PUBLIC_VERIFIER_CONTRACT=C...

# SECRET — server-only, never prefix with NEXT_PUBLIC_
# Testnet deployer account — used only for demo XLM fund flow
# Never commit this file. Add .env.local to .gitignore.
ISSUER_SECRET=S...
